ssl_cert_dir: "/etc/ssl/openssl/certs"
ip_address: "{{ vm_ip_address }}"

media_base_path: "{{ base_app_storage_path }}/media"
media_data_path: "{{ my_non_cloud_nas_storage_path }}/Media"
media_local_ssd_path: "{{ base_local_ssd_storage_path }}/Media"

jellyfin_base_path: "{{ media_base_path }}/jellyfin"
jellyfin_config: "{{ jellyfin_base_path }}/config"

jellyfin_local_ssd_path: "{{ media_local_ssd_path }}/jellyfin"
jellyfin_cache: "{{ jellyfin_local_ssd_path }}/cache"
jellyfin_logs: "{{ jellyfin_local_ssd_path }}/logs"
jellyfin_metadata: "{{ jellyfin_local_ssd_path }}/metadata"

sonarr_base_path: "{{ media_base_path }}/sonarr"
sonarr_config: "{{ sonarr_base_path }}/config"

sonarr_local_ssd_path: "{{ media_local_ssd_path }}/sonarr"
sonarr_logs: "{{ sonarr_local_ssd_path }}/logs"
sonarr_metadata: "{{ sonarr_local_ssd_path }}/MediaCover"

radarr_base_path: "{{ media_base_path }}/radarr"
radarr_config: "{{ radarr_base_path }}/config"

radarr_local_ssd_path: "{{ media_local_ssd_path }}/radarr"
radarr_logs: "{{ radarr_local_ssd_path }}/logs"
radarr_metadata: "{{ radarr_local_ssd_path }}/MediaCover"

# config_path: "{{ media_base_path }}/config"


jellystat_path: "{{ config_path }}/jellystat"
jellystat_config: "{{ jellystat_path }}/config"
jellystat_db_config: "{{ config_path }}/postgres_db"
jellystat_db_username: "{{ vm_username }}"
jellystat_db_password: "{{ vm_password }}"
jellystat_jwt_key: "{{ vm_password }}"



prowlarr_config: "{{ config_path }}/prowlarr"
jellyseerr_config: "{{ config_path }}/jellyseerr"

nzbget_config: "{{ config_path }}/nzbget"
nzbget_username: "{{ vm_username }}"
nzbget_password: "{{ vm_password }}"

qbittorrent_config: "{{ config_path }}/qbittorrent"


aria_config: "{{ config_path }}/aria"
aria_domain: "aria.{{ subdomain }}"
aria_secret: "{{ vm_password }}"
flaresolverr_domain: "flaresolverr.{{ subdomain }}"
jellyfin_domain: "jellyfin.{{ subdomain }}"
jellyseerr_domain: "jellyseerr.{{ subdomain }}"
jellystat_domain: "jellystat.{{ subdomain }}"
lunasea_domain: "lunasea.{{ subdomain }}"
nzbget_domain: "nzbget.{{ subdomain }}"
prowlarr_domain: "prowlarr.{{ subdomain }}"
qbittorrent_domain: "qbittorrent.{{ subdomain }}"
radarr_domain: "radarr.{{ subdomain }}"
sonarr_domain: "sonarr.{{ subdomain }}"


